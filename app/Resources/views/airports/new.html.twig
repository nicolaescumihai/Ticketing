{% extends 'base.html.twig' %}

{% block body %}
    <h1>Airport creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}<br>
        <input type="submit" value="Create" />
    {{ form_end(form) }}
    <br>
    <ul>
        <li>
            <a href="{{ path('airports_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
