{% extends 'base.html.twig' %}

{% block body %}
    <h1>Airport Gate Creation</h1>

    {{ form_start(form) }}

    
        {{ form_widget(form) }}<br>
        <input type="submit" value="Create" />

    {{ form_end(form) }}
<br>
    <ul>
        <li>
            <a href="{{ path('airportgates_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
